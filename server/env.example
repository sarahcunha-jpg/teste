PORT=3001
# Origens permitidas (separadas por vírgula). Use * apenas em desenvolvimento.
CORS_ORIGIN=https://sarahcunha-jpg.github.io
# Caminho do banco SQLite (relativo ou absoluto). Em produção no Render use ./data/frota.db
DB_PATH=./data/frota.db
